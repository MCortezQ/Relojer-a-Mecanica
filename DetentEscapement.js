class DetentEscapement {
    constructor(pendulum, escapeGear, system) {
        console.log("⚠️ Escape de Cronómetro (Detent) - Pendiente de implementación");
        // ... código mínimo para no romper
    }
}